# rule: a ref field needs a 'target' (here it is absent)
schema:
  types:
    net.thing:
      key:
        name: { type: string }
      fields:
        name: { type: string, required: true }
        owner: { type: ref }
